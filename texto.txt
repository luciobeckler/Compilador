{
     bool j; bool i; bool x; bool y;
    i = true;  //Corrigir na análise lexa gerando erro ao colocar false
    j = 13; 
    i = i + j;
    x = true; 
    y = true;
    while ( x < y )  {
        i = i + j;
        if ( x < y ){
        }
    }
}